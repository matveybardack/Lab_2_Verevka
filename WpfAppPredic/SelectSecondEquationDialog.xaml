<!-- SelectSecondEquationDialog.xaml -->
<Window x:Class="WpfAppPredic.SelectSecondEquationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выберите второе уравнение"
        Height="200"
        Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Выберите второе уравнение для бинарного оператора:" />

        <ComboBox Grid.Row="1"
                  x:Name="EquationsComboBox"
                  Margin="0,10,0,10"
                  Height="30" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="OKButton_Click" />
            <Button Content="Отмена"
                    Width="80"
                    Height="30"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>